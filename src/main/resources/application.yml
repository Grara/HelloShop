spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/porfolshop
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 10MB

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace

fileDir: C:/test/

testTxt: test